
  # Admin Panel for Teachers (Copy)

  This is a code bundle for Admin Panel for Teachers (Copy). The original project is available at https://www.figma.com/design/hxm9lneSyMScon9XYfpYId/Admin-Panel-for-Teachers--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  